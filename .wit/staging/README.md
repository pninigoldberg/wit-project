# WIT – Basic Version Control System

A simplified version control system inspired by Git, built in Python using a command-line interface (CLI).

This project was developed as part of the WIT Final Assignment.

---

## 📌 Project Description

WIT is a basic version control system that mimics the core behavior of Git.

It allows users to:
- Initialize a repository
- Add files to a staging area
- Create commits (snapshots)
- Check repository status
- Restore previous versions

All metadata is stored inside a hidden `.wit` directory.

---

## 🛠 Technologies Used

- Python 3.x
- Click (for CLI management)
- os
- shutil
- datetime
- hashlib (for commit IDs)
- JSON / file-based metadata storage

---
